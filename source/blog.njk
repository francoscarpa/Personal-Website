---
layout: RootLayout.njk
title: "Blog - Franco Scarpa"
description: "Blog page of Franco Scarpa’s website."
h1: "Blog"
tags: MainNavPages
order: 2
---
{%- for post in collections.Posts | reverse -%}
<div class="PostListEntry">
   <p>Published on {{ post.data.created.toLocaleDateString("en-EN") }}</p>
   <p>Last update on {{ post.data.updated.toLocaleDateString("en-EN") }}</p>
   <p>
      <a href="{{ post.url }}">{{ post.data.h1 | safe }}</a>
   </p>
</div>
{%- endfor -%}